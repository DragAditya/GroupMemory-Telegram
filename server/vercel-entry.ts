import { createGroupMemoryApp } from "./app";

// The Vercel build bundles this entry point and all internal server modules into
// an ESM catch-all function under api/[...path].mjs.
export default createGroupMemoryApp();
